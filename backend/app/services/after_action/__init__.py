"""After-action review (Phase 7 — Learn).

Compares a strategy's PREDICTED performance (Praxis at-risk ranking) against the
ACTUAL RECORDED impact of the real historical event (DesInventar), extracts
lessons, and persists them so doctrine improves across events.

Honest framing (enforced in code, UI, and docs): the recorded side is the actual
historical response baseline — NOT the outcome of executing a Praxis playbook.
This is a retrospective *predicted-vs-recorded* benchmark. See
docs/AFTER_ACTION.md.
"""
